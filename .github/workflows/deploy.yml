name: Build and Deploy
on:
    push:
        branches: [main]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v6
              with:
                  persist-credentials: false
            - name: Setup node
              uses: actions/setup-node@v6
              with:
                  node-version: 24
            - name: Setup pnpm
              uses: pnpm/action-setup@v5
            - name: Build
              run: |
                  node -v
                  npm -v
                  pnpm -v
                  node scripts/init.mjs
                  node scripts/build.mjs pro
                  cd dist
                  zip -qr ui.zip *
            - name: Deploy
              uses: wlixcc/SFTP-Deploy-Action@v1.2.4
              with:
                  username: ${{ secrets.TENCENT_SERVER_USER }}
                  server: ${{ secrets.TENCENT_SERVER_HOST }}
                  ssh_private_key: ${{ secrets.TENCENT_SERVER_KEY }}
                  local_path: './dist/ui.zip'
                  remote_path: '/data/app/binaries'
                  sftpArgs: '-o ConnectTimeout=5'
            - name: Executing Remote SSH Commands
              uses: appleboy/ssh-action@v1.0.3
              with:
                  host: ${{ secrets.TENCENT_SERVER_USER }}
                  username: ${{ secrets.TENCENT_SERVER_KEY }}
                  key: ${{ secrets.SERVER_KEY }}
                  script: |
                      sh /data/tools/deploy-ui.sh
